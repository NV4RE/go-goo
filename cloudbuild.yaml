steps:
  - name: "sonarsource/sonar-scanner-cli:latest"
    entrypoint: "sh"
    args:
      - "-c"
      - |
        sonar-scanner -Dsonar.host.url=https://sonarqube.nv4re.com -Dsonar.login=11ba84666f059feaf07d7327c944f0c7eb0869e1 -Dsonar.projectKey=world -Dsonar.sources=. || true
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/$REPO_NAME", "."]
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/$REPO_NAME"]
